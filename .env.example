# Copy to .env and fill in. .env is gitignored and never committed.
# In CI (GitHub Actions), set the same name as a repository secret instead.
# OPTIONAL: without a token the service still runs, just with tighter rate limits.
STOCKTWITS_TOKEN=
